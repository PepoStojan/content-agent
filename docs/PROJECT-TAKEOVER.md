# SEO Content Maker — Project Takeover Document

Durable current-state reference for a fresh Claude Code session. Generated by reading the repository directly (git history, migrations, source, architecture docs) — nothing here is inferred or assumed beyond what the repo shows. If anything below conflicts with what you observe in the live repo, trust the repo and update this document.

---

## CURRENT TRUTH

### 1. Product purpose

SEO Content Maker is a Next.js + Supabase application that runs a managed, multi-stage SEO content production pipeline for an organization's team: **Strategy Brief → Content Blueprint → Content → QA → Export**, each stage AI-assisted (Anthropic), versioned, and gated behind human approval. It is built for a small internal team with three roles (`team_lead`, `seo_manager`, `content_writer`) working across many concurrent projects ("pieces of content"), each fed by uploaded competitor/SERP research and existing-site knowledge, and constrained by a per-organization Business Profile and Brand Voice.

### 2. Current architecture

- **Framework:** Next.js 16 (App Router, Turbopack), React 19, TypeScript.
- **Backend:** Supabase (Postgres, Auth via Google OAuth, Storage, RLS as the authorization boundary).
- **UI:** Tailwind v4 + shadcn/ui (`base-nova` style), a locked Design V1 token system.
- **AI:** `@anthropic-ai/sdk` (Anthropic only — no multi-provider abstraction, D5).
- **Monitoring:** Sentry (`@sentry/nextjs`).
- **No background job runner.** Vercel Workflow was evaluated and explicitly deferred (D3); everything runs on plain Next.js Server Actions today.
- **Multi-tenant model:** `organizations` → `profiles` (role-scoped) → `projects`, enforced end-to-end by Postgres RLS, not just application logic.
- **Source of truth discipline:** all TypeScript enum/union types are derived from generated Supabase types (`lib/supabase/types.ts`, via `npx supabase gen types typescript --linked`), never hand-declared. This was a deliberate cleanup pass after several hand-typed unions silently drifted from the real DB enums.

### 3. Completed phases and their commit hashes

Full `git log --oneline` on `main` (5 commits, matches `origin/main` exactly as of this writing):

| Commit | Phase | Summary |
|---|---|---|
| `0ef36b9` | Phase 0 | chore: initialize SEO Content Maker foundation — Next.js/Supabase scaffold, Design V1 tokens, 13 initial migrations (`20260817000001`–`20260817000013`) |
| `cd30bd6` | Phase 1 | feat: add Google authentication |
| `d8c363a` | Phase 2 | feat: add business and brand profiles |
| `a50cdf6` | Phase 3 | feat: complete phase 3 project creation workflow |
| `1483bd3` | Phase 4.1 + 4.2 | feat: add generation engine infrastructure |

Note: Phase 4.1 (generation state machine) and Phase 4.2 (Generation Engine) were both folded into the single `1483bd3` commit rather than committed separately — confirmed by that commit containing both `20260821000001_phase4_1_generation_infrastructure.sql` and the two `20260822000001`/`...002_phase4_2_...` migrations.

### 4. Current Git state

- Branch: `main`, up to date with `origin/main` (`git@github.com:PepoStojan/content-agent.git`).
- Working tree: clean except one untracked file, `docs/architecture/phase-4-3-strategy-brief-plan.md` (a planning document, not yet committed).
- No open branches other than `main`. No uncommitted code changes.

### 5. Current database/migration state

19 migration files exist, in `supabase/migrations/`, applied in order (user applies via `npx supabase db push`; Claude cannot run this interactively — no DB password access. Claude generates types via `npx supabase gen types typescript --linked`, which does work without a password):

1. `20260817000001_extensions_and_enums.sql` — all core enums (`content_type`, `project_status`, `project_file_type`, `file_validation_status`, `upload_status`, `research_source_type`, `artifact_version_status`, `content_version_status`, `generation_run_type`, `generation_run_status`, `user_role`), shared `set_updated_at()` trigger.
2. `20260817000002_core_tenancy.sql` — `organizations`, `profiles`, `business_profiles`, `brand_profiles`, `settings`.
3. `20260817000003_projects.sql` — `projects`, `project_members`.
4. `20260817000004_files_and_research.sql` — `project_files`, `research_packages`, `research_sources`.
5. `20260817000005_website_knowledge.sql` — `website_datasets`, `website_urls`, `internal_link_candidates`.
6. `20260817000006_generation_runs.sql` — `generation_runs` (created empty/unused until Phase 4.2).
7. `20260817000007_brief.sql` — `content_briefs`, `brief_versions`, `brief_topics`, `brief_internal_links`.
8. `20260817000008_blueprint.sql` — `content_blueprints`, `blueprint_versions`, `blueprint_nodes`.
9. `20260817000009_content.sql` — `content_documents`, `content_versions`.
10. `20260817000010_qa.sql` — `qa_reports`, `qa_report_content_versions`, `qa_findings`.
11. `20260817000011_export.sql` — `exports`, `export_content_versions`, `export_files`.
12. `20260817000012_audit_log.sql` — `audit_log`.
13. `20260817000013_enable_rls.sql` — enables RLS on every tenant-scoped table, **zero policies** (default-deny; only the service-role client could read/write anything at this point).
14. `20260818000001_phase2_provisioning_and_rls.sql` — Phase 2 RLS policies (organizations/profiles/business_profiles/brand_profiles).
15. `20260819000001_phase3_projects_rls_and_storage.sql` — Phase 3 RLS policies (projects, project_members, project_files, research_packages/sources, website_datasets/urls, internal_link_candidates) + Storage bucket setup.
16. `20260820000001_fix_rls_helper_functions.sql` — bugfix: made `current_organization_id()`/`current_app_role()` `SECURITY DEFINER` with fixed `search_path`.
17. `20260820000002_fix_can_access_project_recursion.sql` — bugfix: same treatment for `can_access_project()`, fixing a real RLS recursion bug (see §20).
18. `20260821000001_phase4_1_generation_infrastructure.sql` — `generation_state` enum, `generation_stage` enum, `generation_events` table, `projects.generation_state` column.
19. `20260822000001_phase4_2_generation_engine.sql` + `20260822000002_phase4_2_generation_lock_index.sql` — Generation Engine schema: `generation_run_status` gains `provider_completed`/`artifact_persisted`, `succeeded` renamed to `completed`; `generation_runs.model_id`→`model`, `finished_at`→`completed_at`; full D7 telemetry columns added; RLS policies for `generation_runs`; partial unique index (D2) in its own transaction (Postgres forbids using a newly-added enum value in an index predicate within the same transaction it was added in, SQLSTATE 55P04 — this is why it's a separate file).

**Rule for all future migrations, already established and must continue:** never modify an already-applied migration file. Every change is a new, additive file.

### 6. RLS/security model

Every tenant-scoped table has RLS **enabled** (migration 13). Policies are added incrementally per phase, reusing two `SECURITY DEFINER` helper functions with fixed `search_path = public`:
- `current_organization_id()` — resolves the caller's org from `profiles`.
- `current_app_role()` — resolves the caller's role from `profiles`.
- `can_access_project(target_project_id)` — true if the project's org matches the caller's org, and either the caller is `team_lead`/`seo_manager` or is listed in `project_members`.

Standard policy shape per table: SELECT via `can_access_project(project_id)` (or an equivalent org/membership check for org-level tables); INSERT/UPDATE gated to `current_app_role() in ('team_lead','seo_manager')`.

**Current policy coverage, verified directly against the migrations (not assumed):**

| Table | RLS enabled | Policies exist |
|---|---|---|
| organizations, profiles | yes | yes (1 each) |
| business_profiles, brand_profiles | yes | yes (4 each) |
| projects | yes | yes (3) |
| project_members, project_files | yes | yes (2, 3) |
| research_packages, research_sources | yes | yes (3, 2) |
| website_datasets, website_urls | yes | yes (3, 2) |
| internal_link_candidates | yes | yes (2) |
| generation_runs | yes | yes (3, Phase 4.2) |
| **content_briefs, brief_versions, brief_topics, brief_internal_links** | yes | **NO — zero policies (fully locked)** |
| content_blueprints, blueprint_versions, blueprint_nodes | yes | no (not yet built) |
| content_documents, content_versions | yes | no (not yet built) |
| qa_reports, qa_report_content_versions, qa_findings | yes | no (not yet built) |
| exports, export_content_versions, export_files | yes | no (not yet built) |
| settings, audit_log | yes | no (not yet built) |

The brief-table gap is a **known, previously-identified blocker** for the next implementation phase (Strategy Brief generation) — see NEXT ACTION. The blueprint/content/qa/export gaps are expected (those phases haven't been built yet).

### 7. Routes and major UI surfaces

- `app/page.tsx` — root/landing.
- `app/sign-in/` — Google OAuth sign-in.
- `app/auth/callback/route.ts`, `app/auth/sign-out/route.ts` — OAuth handlers.
- `app/pending-access/page.tsx` — shown when an authenticated user has no `profiles` row yet.
- `app/(app)/layout.tsx` — authenticated app shell.
- `app/(app)/dashboard/page.tsx` — project list, status badges (derived from `project_status`).
- `app/(app)/business-profiles/` — CRUD (list/detail/edit), click-card → detail → edit pattern.
- `app/(app)/brand-voice/` — CRUD, same pattern.
- `app/(app)/new-content/` — project creation wizard (`new-content-wizard.tsx` + `actions.ts`). Ends at project creation only — does **not** trigger any generation (a deliberate Phase 3 boundary decision).
- `app/(app)/projects/[id]/page.tsx` — the Project Workspace shell: header, status badge, 5-tab bar (Brief Review / Blueprint / Content Editor / QA / Export — currently all but "Brief Review" are visually locked), a pipeline progress card, and (for managers only) `generation-test-controls.tsx`, a Phase 4.1/4.2 manual-transition test harness. The Brief Review tab currently renders only a static "not implemented yet" placeholder — no real Brief UI exists yet.
- `app/(app)/settings/page.tsx` — present, minimal.

### 8. Project/workspace flow

1. User creates a project via the New Content wizard (`app/(app)/new-content/`) — sets name, content type, market, target query/topic, links a Business Profile and Brand Profile, and can upload research/website-knowledge files. Wizard ends with `project_status = 'ready_for_brief'` (or earlier states like `draft`/`ingesting` depending on upload state) — it explicitly does not start generation.
2. User lands in the Project Workspace (`/projects/[id]`), starting on the Brief Review tab.
3. Real generation (Strategy Brief onward) has not been implemented yet — this is the next phase of work.
4. Two parallel state systems track project progress and must eventually both be kept in sync by generation code (not yet wired to each other anywhere):
   - `projects.status` (`project_status` enum, Phase 0) — the coarse, Dashboard-facing state machine: `draft → ingesting → ready_for_brief → brief_generated → brief_changes_requested → brief_approved → blueprint_generated → ... → exported`, or `failed`.
   - `projects.generation_state` (`generation_state` enum, Phase 4.1) — the fine-grained pipeline state machine (see §9). Nothing currently advances `project_status` past `ready_for_brief` in application code.

### 9. Generation state machine

Defined in `lib/generation/state-machine.ts`, backed by the `generation_state` Postgres enum (15 values) and `generation_stage` enum (5 values: `strategy`/`blueprint`/`content`/`qa`/`export` — deliberately excludes `draft`, since nothing generates during draft).

- Linear happy-path: `draft → strategy_pending → strategy_generating → strategy_completed → blueprint_pending → ... → export_completed`.
- Any `*_generating`/`*_running` state can transition to `failed`.
- `failed` can only re-enter at a stage's own `*_pending` state (manual retry decision, never automatic).
- `eventStageForTransition(from, to)` resolves the correct `generation_events.stage` value for a transition into `failed` by looking at `from` (fixed a real bug where this was previously hardcoded to `"draft"`, an invalid value for `generation_stage`, causing a live 500 error).
- `transitionGenerationState()` (`lib/generation/actions.ts`) is the single write path: validates the transition via `canTransition()`, updates `projects.generation_state`, and inserts a `generation_events` row (timeline log, separate concept from `generation_runs` — see D1/D7 doc §1's "Challenge" note: `generation_runs` is the per-attempt job record with cost/telemetry, `generation_events` is the pipeline timeline, including non-generation events like a human clicking Approve).

### 10. Generation Engine

`lib/generation/engine.ts` (Phase 4.2 skeleton) — manages `generation_runs` rows only, with **zero knowledge of what a Strategy Brief/Blueprint/etc. actually is**. Current exported functions, exact as shipped in `1483bd3`:

- `startGeneration({ projectId, type, inputRef?, retryOfGenerationRun? })` — inserts a `running` row; the partial unique index (D2) is the actual duplicate guard, this function just turns the resulting `23505` constraint violation into a clean `{ status: "already_active" }` result instead of a raw error.
- `completeGeneration({ generationRunId, projectId, telemetry, outputRef?, metadata? })` — **currently a single call that sets `provider_completed_at`/`artifact_persisted_at`/`completed_at` all to the same timestamp in one update.** The function's own doc comment says this is "correct for the skeleton, where no artifact is actually being written yet" and that a real stage needing to persist a version row *between* receiving the provider response and finishing would need to call it in two steps instead — **this has not been built yet and is a known required change for the next phase** (see NEXT ACTION).
- `failGeneration({ generationRunId, projectId, error, telemetry? })` — sets `status='failed'`, `error` jsonb, partial telemetry if available, `completed_at`/`duration_ms`.
- `recoverGeneration(generationRunId)` — inspects an interrupted run and repairs it without ever recalling the provider: `artifact_persisted` → mechanically flips to `completed`; `provider_completed` → returns `needs_persist` (does not attempt to persist itself — stage-specific, deliberately out of the engine's scope); `queued`/`running` with no `output_ref` → marked `failed`, retryable; already-terminal → no-op.

All four functions are role-gated via `assertCanRunGenerations()` (`team_lead`/`seo_manager` only).

### 11. Locked architecture decisions D1–D7

From `docs/architecture/phase-4-2-generation-engine.md`, verbatim:

| # | Decision | Status |
|---|---|---|
| D1 | `output_ref`-first persistence is the required architecture for every Generation: raw provider output is durably stored before any structured parse/insert is attempted. | LOCKED |
| D2 | `generation_runs` must enforce one active Generation per (project, type) via a partial unique index (`WHERE status IN ('queued','running','provider_completed','artifact_persisted')`) — the single mechanism preventing duplicate-click, refresh-resubmit, and simultaneous-user races. | LOCKED |
| D3 | Vercel Workflow is explicitly deferred; build on plain Server Actions. Revisit only on a demonstrated requirement, not default momentum. | LOCKED |
| D4 | No DB-stored/editable prompts; prompt versions stay file-based (`lib/ai/prompts/<stage>/v1.ts`). | LOCKED (reaffirmed) |
| D5 | No multi-provider abstraction interface; the stage function itself is the only abstraction boundary. | LOCKED (reaffirmed) |
| D6 | No streaming, no agentic tool-use loops, no cost dashboard/budget/rate-limiting, no separate cost ledger table, in this phase. | LOCKED (reaffirmed) |
| D7 | Generation Telemetry — every `generation_runs` row permanently records `provider`, `model`, `provider_request_id`, `input_tokens`, `output_tokens`, `total_tokens`, `estimated_cost_usd`, `started_at`, `provider_completed_at`, `artifact_persisted_at`, `completed_at`, `duration_ms`, `finish_reason`, `attempt_number`, `retry_of_generation_run`, `metadata`. Capture only — no dashboards/reporting/billing. | LOCKED |

Note the migration's index predicate (D2, as actually shipped) is broader than the decision log's original wording — it includes `provider_completed`/`artifact_persisted`, not just `queued`/`running` — because those two recovery states are still "in flight," a refinement made when D1's phase model was finalized. The shipped index is authoritative over the summary table text.

---

## LOCKED DECISIONS

(Architecture and process rules established and reaffirmed across the session — not subject to casual relitigation.)

- **Versioned artifacts, head+version pattern.** Every generated artifact (Brief, Blueprint, Content, etc.) has a head table (e.g. `content_briefs`) with a `current_version_id` pointer, and an immutable, append-only versions table. Nothing is ever overwritten or deleted; regeneration always inserts a new version and moves the pointer.
- **Approval gates + provenance labeling.** A single global strict-approval-gate model. Every field in generated artifacts must be labeled as one of: Research finding (immutable, sourced from `research_sources`), AI recommendation (editable, Claude's synthesis), User decision, or System validation. This distinction is not cosmetic — it determines which app code is allowed to write which field (see `brief_topics` vs `brief_versions.entities_concepts` in the Phase 4.3 plan).
- **AI provider:** Anthropic only, no abstraction layer beyond the stage function itself (D5).
- **Prompts are file-based, never DB-stored** (D4).
- **No background job runner** (Vercel Workflow explicitly deferred, D3) — plain Server Actions.
- **RLS is the real authorization boundary, not UI state.** Explicitly relearned twice in this project (disabled buttons are not security; hidden UI elements are not security) — every table gets real Postgres policies, not just app-level role checks.
- **All new/changed RLS helper functions must be `SECURITY DEFINER` with `SET search_path = public` from day one** — mandatory pattern after the Phase 3 recursion bug (§20).
- **Migrations are strictly additive.** Never edit an applied migration; always create a new file.
- **TypeScript enum/union types are always derived from generated Supabase types**, never hand-declared duplicates of a Postgres enum.
- **Never print, commit, log, expose, or include the Anthropic API key in source code or diagnostic output**, in any phase from Phase 4.3 onward. Key lives in `.env.local` only.

---

## DEFERRED

Explicitly out of scope until a real, demonstrated need arises — do not build ahead of it:

- Vercel Workflow / any background job runner (D3).
- Multi-provider AI abstraction interface (D5).
- Streaming responses, agentic tool-use loops (D6).
- Cost dashboard, budget/rate-limiting UI, a separate cost-ledger table, billing reporting (D6). Telemetry is captured (D7) but not surfaced anywhere yet.
- DB-stored or user-editable prompts (D4).
- CSV and DOC/DOCX research file parsing — files are stored, but not really parsed; only the Markdown research format has a real parser (`lib/ingestion/markdown-research-parser.ts`).
- Real sitemap.xml / Screaming Frog `internal_all.csv` parsing for Website Knowledge — `website_urls`/`internal_link_candidates` ingestion is simulated (files stored, no real extraction), meaning both tables are effectively empty for every real project today.
- Blueprint, Content, QA, and Export generation stages (schema exists, RLS policies do not, no stage code exists).
- Section-level regeneration (only meaningful once Content exists).
- "Request changes" behavior for any artifact — the button exists in the Design V1 UI prototype with no wired behavior anywhere (same class of gap as Business/Brand Profile's originally-unwired Edit/Delete).

---

## KNOWN ISSUES

- **`content_briefs`, `brief_versions`, `brief_topics`, `brief_internal_links` have RLS enabled with zero policies.** Blocking: no authenticated read or write is possible against these tables until a policy migration is added (reusing existing `can_access_project`/`current_app_role` helpers — no new helper function needed).
- **`completeGeneration()` collapses three distinct D1 recovery phases into one atomic call.** It does not yet support genuinely separating "provider responded" from "artifact persisted," which the next real stage (Strategy Brief) needs in order for D1's resilience guarantee to actually hold in practice. **Resolved as a locked decision (2026-08-18):** the API split is approved exactly as `recordProviderCompleted()` → `recordArtifactPersisted()` → `completeGeneration()`, per `docs/architecture/phase-4-3-strategy-brief-plan.md`'s "Locked decisions" section. Not yet implemented — still a required Phase 4.3 code change, no longer an open design question.
- **`ANTHROPIC_API_KEY` and `ANTHROPIC_MODEL` are both blank in `.env.local`**, confirmed directly (not from memory) at the time this document was written. Approved model per explicit user instruction: `claude-sonnet-4-6`. Nothing will work until these are set locally by the user — Claude should never ask the user to paste the key into chat.
- **`brief_versions.model_id` was never renamed to `model`**, unlike `generation_runs.model_id`→`model` in the Phase 4.2 migration — a naming inconsistency, cosmetic, not blocking.
- **`internal_link_candidates` and `website_urls` have real RLS policies but effectively no real data** — website knowledge ingestion is simulated, so any feature reading from these tables will see empty results for real projects until real parsing is built.
- **Two parallel state machines (`projects.status` and `projects.generation_state`) are not wired to each other anywhere in application code.** Nothing currently advances `project_status` past `ready_for_brief`; the Dashboard badge will silently stop reflecting real progress once generation starts unless a stage explicitly updates both.
- **`brief_topics` vs `brief_versions.entities_concepts`/`questions` look redundant in the schema but encode different provenance** (Research finding vs AI recommendation respectively) — a naive single-LLM-call implementation would mislabel research data as an AI opinion. Any Brief-generation implementation must derive `brief_topics` deterministically from `research_sources`, not from the Claude call.

---

## 20. Critical lessons / root-cause findings from previous debugging

Preserved verbatim in intent from the migrations' own comments — these are hard-won, not generic advice:

1. **RLS recursion bug (`can_access_project`).** Root cause: `can_access_project()` ran as `SECURITY INVOKER` while also being the `USING` expression of `projects_select_accessible` on the very table it queried. `ensureDraftProject()`'s `.insert(...).select("id")` forced a RETURNING-clause SELECT-policy check on the new row, which called `can_access_project()`, which queried `projects` again under the same policy — a recursive, RLS-governed self-reference. This produced the exact same error text as a `WITH CHECK` failure ("new row violates row-level security policy for table projects") even though the INSERT's own `WITH CHECK` was passing — a genuine footgun for diagnosis, since the error message pointed at the wrong clause. Every other hypothesis (identity/value mismatch, the policy itself, missing triggers, `profiles`' own RLS) was ruled out first. Fix: `SECURITY DEFINER` + fixed `search_path` + schema-qualified references, so internal queries bypass RLS re-evaluation instead of recursing.
2. **The same root cause class hit `current_organization_id()`/`current_app_role()` first**, one migration earlier — as plain functions with no fixed `search_path`, their internal query against `profiles` ran under the caller's privileges/search_path at evaluation time *inside another table's RLS policy check*, resolving inconsistently from how they resolved when called directly.
3. **Postgres will not let you use a newly-added enum value inside an index predicate in the same transaction it was added in** (SQLSTATE 55P04) — forced splitting the Phase 4.2 migration into an enum/columns file and a separate index file.
4. **`Database = any` placeholder types hid several real bugs at compile time.** Regenerating real types via `supabase gen types typescript --linked` (works without the DB password) immediately surfaced multiple hand-typed-union drifts from the real DB enums — fixed by deriving every such type from `Database["public"]["Enums"][...]` instead.
5. **A `redirect()` call inside a Server Action can be caught by a client-side try/catch and shown to the user as an error** (`NEXT_REDIRECT`) — fixed using `unstable_rethrow(e)` from `next/navigation` at the top of the catch, per Next.js's documented pattern for this footgun.
6. **A file input's `.files` read from a ref at a later event (e.g. click-time) can return `undefined`** even though a file is visibly selected — fixed by capturing the file immediately in `onChange` via `useState<File | null>`, not reading a ref later.
7. **Renaming an enum value cascades silently.** Renaming `generation_run_status`'s `succeeded`→`completed` broke `lib/generation/actions.ts`, which reused that same enum for `generation_events.status` with a hardcoded `"succeeded"` string literal — caught only by re-reading the file, not by any type error (the literal was still a valid string at the time). Lesson: grep for every literal use of an enum value before renaming it in a migration.

---

## 21. Rules Claude must follow when modifying this project

- Never modify an already-applied migration file. Every schema change is a new, additive file.
- Every new or changed RLS helper function must be `SECURITY DEFINER` with `SET search_path = public` from the start.
- Every new tenant-scoped table must ship with real RLS policies in the same phase that introduces it — "RLS enabled, zero policies" is a known trap in this project, not an acceptable interim state.
- Never hand-declare a TypeScript union/enum type that duplicates a Postgres enum — always derive from `Database["public"]["Enums"][...]` in the generated types.
- Never print, commit, log, expose, or include the Anthropic API key (or any other secret in `.env.local`) in source code, commit messages, or diagnostic output.
- Do not add features, abstractions, or speculative infrastructure beyond what's explicitly requested for the current phase (D3/D5/D6 exist precisely because this was tested and rejected before).
- Run typecheck/lint/build before reporting implementation work complete, using the project's existing strict, minimal report format.
- Migrations are applied by the user (`npx supabase db push`) — Claude cannot do this interactively (no DB password). Claude regenerates types via `npx supabase gen types typescript --linked`, which does work without a password.
- When asked to plan before implementing, treat "no code / no migrations / no API calls" as a hard boundary, not a suggestion — this document itself was produced under that constraint.

---

## NEXT ACTION

The last completed planning artifact is `docs/architecture/phase-4-3-strategy-brief-plan.md` (untracked, not yet committed) — a full Phase 4.3 (Strategy Brief Generation) implementation plan, covering the RLS gap, the `completeGeneration()` API-split requirement, and the provenance split between `brief_topics` and `brief_versions`' AI-generated fields documented above.

**All three previously-open Phase 4.3 decisions are now locked (2026-08-18)**, recorded in the plan doc's "Locked decisions" table:
- **Request changes:** approved → `needs_revision`; never auto-starts generation; user must edit inputs/instructions then explicitly Regenerate; the approved version stays immutable.
- **Generation API split:** `recordProviderCompleted()` → `recordArtifactPersisted()` → `completeGeneration()`, approved as the required D1 persistence lifecycle for `lib/generation/engine.ts`.
- **Regeneration after approval:** allowed; always creates a new `brief_versions` row starting at `status='draft'` requiring separate approval; the prior approved version is never mutated.

No implementation has started. To resume work: set `ANTHROPIC_API_KEY`/`ANTHROPIC_MODEL` (`claude-sonnet-4-6`) in `.env.local`, then begin Phase 4.3 implementation starting with the brief-tables RLS migration (plan §12).
