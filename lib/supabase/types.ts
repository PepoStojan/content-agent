/**
 * Placeholder for generated Supabase types.
 *
 * Once the V1 migrations (supabase/migrations/) have been applied to a
 * real Supabase project, replace this file with the generated types:
 *
 *   npx supabase gen types typescript --project-id <project-ref> > lib/supabase/types.ts
 *
 * Using `Database = any` here is intentional and temporary — it keeps
 * lib/supabase/{client,server,admin}.ts type-checking correctly before
 * a project exists, without fabricating a schema shape by hand.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
